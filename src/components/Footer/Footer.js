import React, { Component } from 'react';
import footer from './footer-fpo.jpg';

export class Footer extends Component {
    render() {
        return (
            <div>
                <img src={footer} alt="" />
            </div>
        );
    }
}

export default Footer;
